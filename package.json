{
  "name": "counsel-ai-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private Model Context Protocol (MCP) server for Counsel AI",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "mcp": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint in root'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.5.4",
    "tsx": "^4.16.2",
    "typescript": "^5.6.3"
  }
}
